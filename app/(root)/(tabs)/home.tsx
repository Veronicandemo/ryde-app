import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      <Text className="text-center text-4xl">Home</Text>
    </SafeAreaView>
  );
};
export default Home;
