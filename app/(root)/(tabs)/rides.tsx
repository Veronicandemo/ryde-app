import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text className="text-center text-4xl">Rides</Text>
    </SafeAreaView>
  );
};
export default Rides;
