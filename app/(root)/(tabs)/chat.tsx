import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () => {
  return (
    <SafeAreaView>
      <Text className="text-center text-4xl">Chat</Text>
    </SafeAreaView>
  );
};
export default Chat;
